type Lobby {
    id: ID! @id
    name: String!

    usersInLobby: [User!]! @relation(link: TABLE)
    rooms: [Room!]! @relation(link: TABLE)

    createdAt: DateTime! @createdAt
    updatedAt: DateTime! @updatedAt
}