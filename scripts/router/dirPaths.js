"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const root = `${__dirname}/../..`;
const scriptsFolder = `${root}/scripts`;
const publicFolder = `${root}/public`;
const webFolder = `${publicFolder}/web`;
const userAvatarsFolder = `${publicFolder}/userAvatars`;
const dirPath = {
    "root": root,
    "scriptsFolder": scriptsFolder,
    "publicFolder": publicFolder,
    "webFolder": webFolder,
    "userAvatarsFolder": userAvatarsFolder
};
exports.default = dirPath;
